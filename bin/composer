#!/usr/bin/env bash

args=""
for arg in "$@"; do
  args="$args $arg"
done

if [[ -f /.dockerenv ]]; then
    cd /srv/minicli && composer ${args}
else
    docker exec -ti -u dev minicli sh -c "cd /srv/minicli && composer $args"
fi